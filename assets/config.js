/* Site config. Update `repo` to "owner/name" once the GitHub repo exists so the
   scoring form can open pre-filled rating issues that the ingest Action picks up. */
window.SBCS_CONFIG = {
  repo: "tristan-mcinnis/sbcs",
  instagram: "https://www.instagram.com/sushi.burger.club/"
};
